# メモリシステム — {棚の名前} の何をどこに置くか（置き場のルール）

> メモリシステムとは「置き場のルール」である。ファイルの中身ではない。孔明はこの紙に**追記できる**（新しい置き場・新しい規則）。**削る・並べ替える・統合する整理は `00廷議/` に起草して承認を得てから。**

## 判定の 3 問（迷ったらこの順）
1. **実体か**（動画・音声・PDF・zip・画像）→ {実体の置き場}。棚には台帳だけ
2. **他人の物か** → `07学習資産/教材ライブラリ/<作者>/<教材>/`（台帳・採った型・文字起こし、全部ここ。分けない）
3. **自社の物か** → 終わっていれば `01`〜`05`。途中なら `08プロジェクト/`

## 内容軸の振り分け
| 何 | どこ |
|---|---|
| 売っている物・その規定・講座 | `01商品資産/` |
| 売り方の素材・活動・成果物（投稿・DM 文面・配布物・メルマガ） | `02マーケティング資産/` |
| 売り物の資産（LP・セールスページ・セールスレター・ウェビナー台本・VSL・クロージング台本・オファー・価格） | `03セールス資産/` |
| 客・質問・知識・便・会議 | `04オペレーション資産/` |
| 契約・経理・人事 | `05バックオフィス資産/` |

## この棚の構造（標準形。AOS設置が器だけ建てた。中身はこの人と孔明が積む）
凡例: `人`＝人が書く ／ `機械`＝機械が定期で書く（書く便が必ず在る） ／ `⛔git`＝git に載せない

```
{棚の名前}/
├── CLAUDE.md                     人   地図だけ。5 KB 以内
├── .gitignore                         10私用/・11一時ファイル/・**/一時ファイル/ を除外
├── .claude/
│   └── rules/                    人   毎回自動で読まれる 3 枚
│       ├── 規矩.md                     ⛔🔒・判断の作法・任せ方・帳簿の列・奏上。**主君だけが変える**
│       ├── メモリシステム.md            どこに置くか（置き場のルール）・入れてはいけない物。孔明が追記できる
│       └── 主君.md                     誰か（名前・呼ばれ方・事業）。3 行だけ。好みも席の名前も書かない
│   帳簿・実験・実行の正本は記録 MCP `kiroku`（追記のみ・焼き先だけ update）。棚には置かない。引き方と書き方は 規矩.md 3・4 節
├── docs/
│   └── SCOPE_PROGRESS.md         機械 8 節。scripts/build_scope_progress.py が生成。手で書かない＝読む直前に撃って作り直す。型の正本は生成器の docstring
├── 00廷議/{件}.md                人   裁定待ちの待合室。1 件 1 枚（7 項目）。推奨なしは入れない。裁定後は 09アーカイブ/裁定/ へ
├── 01商品資産/                    売っている物・その規定・講座
├── 02マーケティング資産/           売り方（投稿・DM 文面・配布物・メルマガ）
├── 03セールス資産/                 売り物の資産。LP・セールスレター・セールスページ・ウェビナー台本・VSL・オファー・価格
├── 04オペレーション資産/           回し方（客・質問・知識・便・会議）
├── 05バックオフィス資産/           契約・経理・人事
├── 06エージェント資産/            席（エージェント）の本文。**空で渡る。**孔明が聞き取って作った席を 1 枚ずつここへ（README.md が地図）
│   │                         ⛔ 棚だけ・DB だけ、にしない＝ここに保存し、あわせてマイエージェントにも登録する
│   ├── {席}.md               人   席の本文の実体。**最初は箱を分けない**（増えて探しにくくなったら孔明が分け方を起草して裁定を仰ぐ）
│   ├── _索引.md              人   この棚の席の一覧（1 席 1 行）
│   ├── _台帳-便.csv          人＋機械 便の台帳（id・cron・時刻・便・正本・agent・状態）。cron と状態は scheduler から機械が書き戻し、**`正本` は人が決める列**
│   ├── _台帳-孔明起動.csv     人   孔明起動の台帳（席・正本・起動条件）。1 行足して生成器を撃つと箱に 1 本増える
│   ├── _便/{HHMM}-{便}.md    機械 エイリアス（symlink・相対）だけ。時計（scheduler）が起こす席。**名前の頭が時刻＝開いた瞬間に何時に何が回るか見える**
│   └── _孔明起動/{席}.md      機械 エイリアスだけ。孔明が起動したら一緒に起きる席。⛔ 同じ席を _便/ と両方に置かない
│                             ⛔ この 2 箱に実体を置かない。生成器 scripts/build_agent_aliases.py（`--check` が門＝迷子・二重起動・実体の居座りを二値で数える／`--selftest` が負制御）
├── 07学習資産/                    他人の物。実体（動画・PDF）は棚に入れない
├── 08プロジェクト/{日付-slug}/status.md  人   動いているプロジェクト（frontmatter の正本は scripts/kiroku.py）
├── 09アーカイブ/                  人が開かない物。検索（RAG）は読む
│   ├── コンテクスト銀行/{key}.md 機械 Vault。他所から抜いた資産 1 物 1 枚（文字起こし・メール・ページの文言・配信の文面・投稿）。平ら・面のフォルダなし
│   │   └── _台帳.csv             機械 抜いた物の全数台帳（面・種類・仕訳先・抜いた理由）。⛔ 内容軸の中に置かない
│   ├── 案件/{日付-slug}.md       人   閉じたプロジェクトの墓標 1 枚（何をした・どうなった・成果はどこへ）。凍結は箱ごと 案件/{日付-slug}/
│   └── 裁定/                     人   裁定の済んだ紙（00廷議/ から移す）
├── 10私用/                  ⛔git 鍵・個人情報・見せない物（席の本文の写しもここ）
├── 11一時ファイル/          ⛔git プロジェクトに属さない途中物。⚠️ 自動では消えないが git にも載らない（→「一時ファイル の規則」）
└── scripts/                  人   道具。kiroku.py（箱の frontmatter）・build_scope_progress.py（全体像）・build_agent_aliases.py（席のエイリアス箱と門）・check-distributable.sh（配布の門）・make-kit.sh（器の切り出し）
    └── git-hooks/            人   pre-commit（core.hooksPath がここを指す。秘密・PII・死んだ参照・箱の語彙）
```

## 台帳と、その横に置く紙

| 何 | 名前 |
|---|---|
| **全数を指す台帳** | **`_台帳.csv`**（索引は `_索引.md`）。⛔ この 2 つは名前を変えない |
| **同じフォルダに種類の違う台帳が要るとき** | **`_台帳-<区分>.csv`**（索引は `_索引-<区分>.md`） |
| **台帳ではない紙**（対応表・設計・履歴） | **何の紙か分かる名前で置いてよい** |

**歯止めは 2 つだけ**（これを守れば名前は自由）。

- ⛔ **1 つの紙は 1 つの役だけ持つ。**「台帳も対応表も兼ねる紙」を作らない（兼ねた瞬間に、どちらの規則で直すか分からなくなる）
- ⛔ **拡張子は中身に合わせる**（表なら `.csv` か `.tsv`／文章なら `.md`）

置き場は「**そのデータを使う人が最初に開くフォルダ**」の直下。⚠️ **台帳を集めた台帳フォルダは作らない**（探す場所が 2 通りになる）。
台帳は全数を指すこと。使われていない行が見えれば、取りこぼしはそこにある。
⚠️ **プロジェクトの中に置いたままにしない**——プロジェクトの箱は閉じるとフォルダごと消える。⇒ 使い続ける紙は仕訳先へ写す。

## 入れてはいけない物（書く前に止まる）

**すぐ古くなる物と、他所に既に在る物は、棚に書かない。リンクにする。**

| 入れない | 代わりに |
|---|---|
| 人名・連絡先・組織図・席の一覧 | 正本（DB・登録簿）を指す |
| 件数・残量・進行中の状態・残り日数 | 撃つコマンドを書く（**日数ではなく日付＋版**） |
| 他の紙に既に在る数字・規則 | その紙を指す。**同じ数字を 2 か所に書かない** |
| 価格・報酬・税・取引先・裁定ログ を外へ出す物へ | ⛔ 規矩 X-4。配るのは器だけ |

## 抜いた資産は、どこへ置くか（2026-09-19 主君裁定・帳簿 id=635・636・637）

> **コンテクスト銀行**＝本文を丸ごと貯める所（棚の中・1 物 1 枚・人は読まない）／**コンテクストストア**＝商品ごとの要約と席の索引（棚の外・DB）

他所（他社の月額サービス）から引き上げた事業資産は、**二層**に置く。**面（預け先）ごとのフォルダは作らない。**

| 層 | どこ | 何が入るか | 人は読むか |
|---|---|---|---|
| **Vault（全数・平ら）** | `09アーカイブ/コンテクスト銀行/<面>-<id>.md` | 抜いた物 1 つにつき 1 枚。本文をそのまま丸ごと。事実の層（面・id・種類・抜いた理由・元の題・元の置き場）は書き換えない | 読まない。検索と席が読む |
| **仕訳先（人が読む）** | `01商品資産`〜`05バックオフィス資産` | 下の対応表で決まる 1 つ。中身はカードと同じ全文（ポインタにしない） | 読む |

**面はフォルダでなく鍵の頭と事実の層に持つ。**`myasp-` で始まる物を数えれば「あの会社から抜いた物」が全部出る。フォルダを足すと同じ物を 2 通りで探すことになる。
⛔ **面の名前にハイフンが入ってよい**（`mail-myasp`・`funnel-utage`）。**鍵を最初のハイフンで割らない**——面と id は frontmatter から引く。

### 面の種類 → 内容軸の対応表（**この表が仕訳の正本。席の本文には写さない**）

| 抜いた物の種類 | 行き先 |
|---|---|
| 講座の講義・本編の動画 | `01商品資産/` |
| 順番に届くメール・配信の文面・SNS の投稿・ブログ | `02マーケティング資産/` |
| 売り込みのページ・申込のページ・値段表・説明会の台本 | `03セールス資産/` |
| 会議の録画・問い合わせ・手順書・よくある質問 | `04オペレーション資産/` |
| 契約書・請求・給与 | `05バックオフィス資産/` |

**迷う物は 3 つだけ**（単発の動画・説明会の台本・無料で配る小冊子）。**この 3 つだけ人に聞き、残りは席が置く。**
⛔ 1 つのカードの仕訳先は 1 つ。両方で使う物は迷う物として人へ。

### 全数の台帳
`09アーカイブ/コンテクスト銀行/_台帳.csv`（列 `key,面,種類,id,題,尺分か字数,原本,カード,仕訳先,抜いた理由`）。**⛔ 内容軸の 1 つに置かない**——抜いた物は 02〜05 にも散るので、全数の台帳が内容軸の中に居座る形になる。`仕訳先` が空＝未仕訳。`面` で束ねると「あの会社から抜いた物が全部在るか」＝**解約してよいかが数えられる**。
面そのものの台帳は **`09アーカイブ/コンテクスト銀行/_台帳-面.csv`**（どのサービスに何を預けているか・月額・解約してよいか）。同じフォルダに種類の違う台帳が 2 本在る形＝「台帳 2 名の例外」に当たる（物ごと＝`_台帳.csv`／面ごと＝`_台帳-面.csv`）。

### ⛔ 棚に入れない物（抜いたときに必ず付いてくる）
**購読者の名簿・配信の宛先・友だちの一覧・購入の履歴。**棚（Vault・仕訳先・台帳・帳簿・ログ）に入れるのは**文章だけ**。名簿は `10私用/`（git 外）か乗り換え先の製品の中へ。抜いた本文の差し込みの穴（`{お名前}` など）を実際の値に置き換えない。

## 正本と写し
- 同じ物を 2 か所に置くなら、どちらが正本かを決め、写しの側に「誰が・いつ書き直すか」を書く
- 人が 1 回書いて終わりの写しは置かない
- 正本を置かない棚を作らない
- 機械が書く紙（`SCOPE_PROGRESS.md`・`_台帳.csv` ほか）は書く便を用意してから置く

## 形
- 深さは 2 段まで。1 フォルダ 1 ファイルを作らない
- 名前は `通し番号_年月_区分_題`。同じ情報を 2 回言わない
- 平らにしたら `_索引.md`（新名 ↔ 元パス）

## 記録は、どこに書くか
| 何 | どこ | 誰が |
|---|---|---|
| 時計が起こした席の実行（走ったか・結果） | 記録 MCP `kiroku` の実行表（`run_start`／`run_end`）。見る窓は `docs/SCOPE_PROGRESS.md` §5、中身の山は `11一時ファイル/` | その席自身。**帳簿に書かずに終わったら失敗**。書き方は下の節 |
| 判断と裁定・失敗・躓き・手が要った | 記録 MCP `kiroku` の帳簿（`ledger_append`） | その席 |
| **運用の型と罠**（手順ではなく「やり方」と「踏むと壊れる物」） | **コンテクストストアの `policy-<名>`（やり方）／`trap-<名>`（踏むと壊れる物）**（置き場は `product/<その人の商品slug>`）。⛔ **棚に紙で置かない** | 書いた席。直したら**読み戻して突き合わせる** |
| プロジェクトの進捗 | そのプロジェクトの `status.md` | owner |
2 か所に同じ事を書かない。書き直すときは古い方を消す。

### 時計が起こした席の実行は、こう書く（`run_start`／`run_end`）

時計（scheduler）が起こした席は、**走り出しに `run_start` を 1 回・終わりに `run_end` を 1 回**。これが「走ったか・どう終わったか」の正本であり、**手が要った率の分母**（規矩 §4）。

| 口 | 渡す物 |
|---|---|
| `run_start` | `seki`＝**`scheduler`**（下の 🚨）／`bin`＝その席の名前／`tenantId`（規矩 §3）。返った `id` を控える |
| `run_end` | `seki`＝`scheduler`（`run_start` と同じ）／`id`＝控えた実行 id／`kekka`＝**`成功`／`失敗`／`手が要った` の 3 語だけ**（表に無い語は server が拒む）／`ledger_ids`＝この回に `ledger_append` を撃っていればその行の id |

| 結果 | いつ選ぶか |
|---|---|
| `成功` | 手順を最後まで通した。**差分なしで終わったのも 成功**（走ったことに変わりはない） |
| `失敗` | 途中で止まった・門が落ちた・書くはずの物を書けなかった |
| `手が要った` | 人の手番が 1 回でも要った（合言葉・確認コード・GO・手で直した）。⚠️ **これが無人化の測り**（規矩 §5-6） |

**中身の数字は帳簿の行に入らない。**`11一時ファイル/便の実行-YYYY-MM.tsv` に 1 行（タブ区切り・無ければ作る）:

```
<開始 ISO>	<席の名前>	<結果>	<実行 id>	<中身>
```

これが `docs/SCOPE_PROGRESS.md` §5 の「実行」の窓になる（⚠️ 自動で消える約束は無いので窓は育ち続ける。**正本は帳簿**）。

- 🚨 **`seki` は `scheduler` の 1 語で完全一致。**⛔ 他の名前を書かない——`scheduler:<席名>` のように足すと**人の便**に落ち、自動の行（＝無人化の分母）から外れる。**書いているのに数えられない**
- ⛔ **`run_end` を撃たずに終わらない。**開いたままの実行は分母から外れる＝走ったことが数えられない
- **帳簿に書かずに終わったら失敗**（旧「ログを書かずに終わったら失敗」の掟をそのまま移した）
- 席が何本あっても `seki` は `scheduler` 固定なので、**`run_last` では席ごとに引けない**（あれは席単位の口）。席ごとに見るのは上の tsv

## プロジェクトは、どう置くか
3 日以上かかる仕事は `08プロジェクト/<YYYY-MM-DD-slug>/status.md` に 1 フォルダ。frontmatter の鍵と語彙の正本は `scripts/kiroku.py` の先頭（`box` で吐き・`--check` で検める・pre-commit が門）:
title / state（起案｜裁定待ち｜実行中｜外部待ち｜確認待ち｜統合可｜完了｜吸収済）/ owner / ask（人に聞く事。書式 問い｜選択肢｜推奨｜決めないと）/ lever（主君の手。何を｜何秒か｜閉じると何が動くか）/ until（外部待ちの中身。何を｜誰から｜期待日）/ resume（閉じた箱の再開の合図。何を｜誰から｜どこで拾う）/ next / absorb（終わったら何がどこへ残るか）/ due / tmp_ttl / updated（道具が入れる）
- `ask` が空でないものは `docs/SCOPE_PROGRESS.md` §2 裁定表に集まる（推奨が無い行は表に出ず「差し戻し」に出る）。⚠️ 正本は `00廷議/<件>.md`（7 項目）
- `updated` が 30 日以上前なら停滞として一覧に出る
- **外部待ち は「相手の答えの後にこちらの続きが在る箱」だけ**。`until` の期待日は日付必須。**こちらの手番が終わった箱（投げ終わり・返事が来たら再開）は 完了 にして `resume` を書き、箱ごと `09アーカイブ/案件/` へ**。孔明が毎朝の廷議で `resume` を引き、来ていたら箱を `08プロジェクト/` へ戻す
- 終わったら absorb 先へ写して、フォルダごと消す（墓標 1 枚を `09アーカイブ/案件/` に残す）
- **凍結**なら `09アーカイブ/案件/<日付-slug>/` へ**箱ごと・中身を 1 本も減らさずに**。再点火する時に「何かが足りない」を起こさないため
- 席から席へ渡す物は「要件のパス＋範囲＋制約＋詰まったら誰へ」の 4 つ。手順は席の本文（`06エージェント資産/`）へ。受け取りを確かめるまでが 1 手

## 一時ファイル の規則（途中物の置き場）
1. **途中物は `一時ファイル/` にしか置かない。**置ける場所は 2 つだけ: `08プロジェクト/<プロジェクト>/一時ファイル/`（プロジェクトの途中物）と `11一時ファイル/`（プロジェクトに属さない試し）。⛔ 棚のルート直下に `一時ファイル/` という名の箱を作らない
2. **`一時ファイル/` は git に載らない。**`.gitignore` に `**/一時ファイル/` と `11一時ファイル/`
3. 🚨 **寿命の約束はしない。**⚠️ **この棚に掃除便の実装は無い**（🔬 2026-09-21 実測: `一時ファイル` を消す実装 0 件・時計の便 0 件）。⇒ ⛔「7 日で消える」と思って置くな・⛔「消えないから安全」と思って一次資料を置くな。**本当の危険は消えることではなく、git に載らないので別の機体には最初から届かないこと**である（🔬 同じ実測で、片方の機体にだけ在った一次資料が 1 件・両方から失われた物が 1 件）。掃除便を作るなら `tmp_ttl` で上書きできる設計にし、**作るまではこの行に「無い」と書いておく**
4. **入れる物**: スクショ・demo の html・変換の中間ファイル・ログ・試し書き・ダウンロードした物。**入れない物**: 成果物・裁定・手順・**一次資料**（`一時ファイル` の外に名前を付けて置く）
   🔑 **迷ったらこの 1 問で決める: 「これが消えたら、誰かの終わりの定義が測れなくなるか？」**——はい＝**`一次資料/`（git に載る／別の機体にも渡る）**／いいえ＝`一時ファイル/`。⛔「あとで要るかも」で `一次資料/` に入れるな（第二の一時ファイル置き場になる）。🩸 2026-09-21: 後続 6 箱が測るのに要る取り込み検体を `一時ファイル/` に置いたため、別の機体では最初から引けず、1 件は**両方の機体から失われて取り直す経路も無かった**
5. **`一時ファイル` の中は名前の規則なし。**外へ出す時に `通し番号_年月_区分_題` を付ける
6. **`一時ファイル` の外に画像・動画・zip・log・`.DS_Store` が在るのは違反。**⚠️ 拾う掃除便は**実装が無い**（2026-09-21 実測）ので、**気づいた席**が `00廷議/` へ 1 枚
7. **閉じる門**: `一時ファイル` を空にしないとプロジェクトは閉じられない。閉じる席が `ls` で確かめる
8. 機械が作って機械がすぐ読み捨てる物（変換の中間など）は、OS の一時領域（`$TMPDIR`）に置いてよい

## 席と便は、棚のルートで起動する
`.claude/rules/` の 3 枚は **起動時の cwd がこの棚のルートのときだけ**自動で載る。途中で `cd` しても載らない。
- 便（scheduler）・monster・サブエージェントは **{棚の置き場} を cwd に起こす**
- 起こす側に cwd を渡せないうちは、その席の手順書の 1 行目に「`.claude/rules/` の 3 枚を読む」を置く（暫定）
- 帳簿に書く・判断を読む・躓いたら 1 行——これは全部 `規矩.md` に在る。**席の本文には写さない**
- **規則は起動時の写しで固定される**: 席は `.claude/rules/` を起動時に 1 回だけ受け取り、途中でディスクを直しても届かない。⇒ 規矩・メモリシステムを直したら、**効くのは次に起動する席から**。対照実験の席は同じ会話から立てず、別プロセス（`claude -p`・cwd=棚ルート）で立てる

## この棚で作らなかった物
- なし。AOS設置は箱を全部作る（空のまま残った箱は、資産を入れる席が終わってから一覧にして主君に聞く）

## 共有ドライブの層（2026-09-24 主君裁定・帳簿 id=1674〜1677）

> **棚は言葉の家、{実体の置き場} は体の家。**形を丸写しにしない。実体の置き場の**上の段は「寿命」だけで分ける**（主題は下の段へ降ろす）。軸が 2 つ並ぶと 1 つの物に家が 2 つできる。

| 上の段（7 つだけ） | 何が入るか | 寿命 |
|---|---|---|
| `00_原本/` | 撮った・落とした生の物。`<取り込み月>/<どこから>/<id>.<拡張子>` で**入れるだけ**。⛔ **主題で分けない**（主題は後から変わる。並べ直しが要る形にしない） | 永久。取り込んだら二度と動かさない |
| `10_制作中/` | 案件ごとの途中物。`<日付-案件名>/` に 1 案件 1 フォルダ | 案件が終われば消える |
| `20_素材庫/` | 何度でも使い回す部品（BGM・効果音・ブランドの素材など）。案件に紐づかない | 永久 |
| `30_資産/` | 出来上がった物。**下は棚と同じ 5 つの名前**（`01商品資産`〜`05バックオフィス資産`）。台帳の仕訳先が分かれば実体の場所も分かる形 | 永久。人が読む・使う |
| `60_外部教材/` | 他人の物（棚の `07学習資産/` と対） | 永久 |
| `80_外部共有/` | 外の人に見せる物だけ。**見せる相手で切る唯一の箱**（共有はフォルダ単位で切れる） | その都度 |
| `90_アーカイブ/` | 年ごと。もう触らない | 永久 |

- ⛔ **既に在る古いアーカイブの箱は、名前も中身も動かさない。**`90_アーカイブ/` の中へ入れるのもやらない（親を変えるだけでも同期が全件を触る）。今の場所のまま「ここは触らない層」として扱う
- **移すのは上の段だけ。命令では 1 本も動かさない**（規矩 X-5）。席がやるのは `mkdir` だけ。フォルダを新しい親の下へ入れるのは人が画面でドラッグ（中身は触らない）。中の深さを浅くするのは、そのフォルダを次に触るときに 1 つずつ
- **ルールの正本はこの節。**実体の置き場の一番上には `_置き場のルールは棚が正本.txt`（ここを指す 1 行）だけを置く。⛔ 同じ規則を実体の置き場に写さない（2 枚になると片方が必ず腐る）
- ⛔ **フォルダの一覧（講座名・作者名・主題名）はここに書かない。**撃てば出る（`ls`）。書いた瞬間から腐る
- ⚠️ **「正本を置かない棚を作らない」の例外**: 7 つの層は**中身が無くても先に全部作る**（資産の引き上げ席がクラウドに繋いだ瞬間に `mkdir`）。理由は 1 つ——**棚は席が書くが、実体の置き場は人が毎日手で書く。**箱が見えなければ人はその場で新しい箱を作り、それが形が増える原因そのもの。棚の空箱は席のためだが、実体の置き場の空箱は人のための道しるべ
