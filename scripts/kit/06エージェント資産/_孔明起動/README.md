# _孔明起動 — 孔明が起動したら一緒に起きる席

**この箱は空で渡される。**

| 何 | 中身 |
|---|---|
| 何がここに入るか | **孔明が起動したときに、時刻に依らず一緒に起きる席へのエイリアス**（`{席}.md`） |
| 誰が張るか | 孔明。席を建てたその場で回し方を聞き、「**孔明が起きたとき**」ならここに 1 本張る |
| 実体はどこか | `06エージェント資産/` の直下（本文）。**⛔ ここに実体を置かない**——正本を 2 つにしない |
| `_便/` との違い | あちらは時計が時刻で起こす物。**⛔ 同じ席を両方に置かない**（二重起動。生成器の `--check` が数える） |
| 誰が読むか | 孔明。起動したらこの箱を見て、並んでいる席を裏で立てる |

張り方: 台帳 `_台帳-孔明起動.csv` に 1 行足して `python3 scripts/build_agent_aliases.py` を撃つ。

⛔ **呼ばれたときだけの席は、どちらの箱にも置かない。**何も書かない。
